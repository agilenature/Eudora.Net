<local:uc_TabBase x:Class="Eudora.Net.GUI.uc_MailboxesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eudora.Net.GUI"
             xmlns:root="clr-namespace:Eudora.Net"
             xmlns:themer="clr-namespace:WpfThemer;assembly=WpfThemer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5,0,0,0" Text="Mailboxes" VerticalAlignment="Center"/>

        <Button x:Name="btn_New" Grid.Row="0" Grid.Column="1" Margin="15,0,5,0" ToolTip="New Mailbox" VerticalAlignment="Center"
                Click="btn_New_Click" Width="60">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <themer:DynamicImage SymbolName="add" Source="pack://application:,,,/GUI/res/images/new/add.png"/>
                    <TextBlock Text="New" VerticalAlignment="Center"/>
                </StackPanel>
            </Button.Content>
        </Button>
        
        <Button x:Name="btn_Delete" Grid.Row="0" Grid.Column="2" Margin="5,0,5,0" ToolTip="Delete Mailbox" VerticalAlignment="Center"
                Click="btn_Delete_Click" Width="60">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <themer:DynamicImage SymbolName="delete" Source="pack://application:,,,/GUI/res/images/new/delete.png"/>
                    <TextBlock Text="Delete" VerticalAlignment="Center"/>
                </StackPanel>
            </Button.Content>
        </Button>

        <ListBox x:Name="Listview"
                 Grid.Row="1"
                 Grid.Column="0"
                 Grid.ColumnSpan="3"
                 SelectionChanged="Listview_SelectionChanged"
                 MouseLeftButtonUp="Listview_MouseLeftButtonUp">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Image Source="{Binding Path=ImageSource}" Width="20" Height="20"/>
                        <Separator Width="10" Background="Transparent" Foreground="Transparent"/>
                        <TextBlock Text="{Binding Path=Name}" FontWeight="Bold" TextAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</local:uc_TabBase>
